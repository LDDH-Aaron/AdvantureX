# Glasses firmware

Place the glasses firmware and sensor integration here. Keep device gesture
recognition local, then send one normalized event to Wingman. The initial event
set is `double_tap`, `long_press`, and `voice`.
